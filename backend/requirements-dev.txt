# Development and test dependencies. Kept separate so the Docker image stays slim.
#   pip install -r requirements-dev.txt

-r requirements.txt

pytest==8.3.4
# Transport behind fastapi.testclient.TestClient.
httpx==0.28.1
