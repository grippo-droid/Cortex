# Normalize line endings: LF in the repository and in working trees, on every
# platform. Keeps diffs clean when the repo is cloned on Windows and Linux.
* text=auto eol=lf

# Binary assets — never normalize these.
*.ico binary
*.png binary
*.jpg binary
*.woff binary
*.woff2 binary
