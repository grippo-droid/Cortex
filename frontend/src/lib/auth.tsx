/**
 * Auth context and route guard.
 *
 * Implemented in T1.4. Token storage is deliberately undecided here: the
 * security doc prefers an httpOnly cookie set by the backend, with an in-memory
 * variable as the acceptable prototype fallback, and localStorage only if it is
 * documented as a known limitation in the README.
 */

export {};
